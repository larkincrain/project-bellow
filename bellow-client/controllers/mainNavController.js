/*
    The main navigation controller for the Bellow controller
*/

(function () {

    var BellowApp = angular.module('BellowApp');

    BellowApp.controller('mainNavController', function (authenticationService, $scope, $rootScope, toaster, $timeout) {

        console.log('In the main nav controller');
    });

})();